(()=>{const KEY='threeCluesV21';function dayIndex(){const p=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Seoul',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date());const g=t=>+p.find(x=>x.type===t).value;return Math.max(0,Math.floor((Date.UTC(g('year'),g('month')-1,g('day'))-Date.UTC(2026,8,14))/86400000));}function resetToday(){try{const raw=localStorage.getItem(KEY);const s=raw?JSON.parse(raw):{};s.stats||={wins:0,streak:0,last:null};s.days||={};const di=dayIndex();s.days[di]={tries:0,revealed:2,won:false,score:0,unlockAt:Date.now()+60000};localStorage.setItem(KEY,JSON.stringify(s));}catch(e){localStorage.removeItem(KEY);}const u=new URL(location.href);u.searchParams.delete('reset');history.replaceState({},'',u.pathname+(u.search?'?'+u.searchParams.toString():'')+u.hash);}const params=new URLSearchParams(location.search);if(params.get('reset')==='1'){resetToday();return;}const MARK='threeCluesV4StateFixed';if(localStorage.getItem(MARK))return;try{const raw=localStorage.getItem(KEY);if(raw){const s=JSON.parse(raw),di=dayIndex();if(s&&s.days&&s.days[di]){s.days[di]={tries:0,revealed:2,won:false,score:0,unlockAt:Date.now()+60000};localStorage.setItem(KEY,JSON.stringify(s));}}localStorage.setItem(MARK,'1');}catch(e){localStorage.removeItem(KEY);localStorage.setItem(MARK,'1');}})();